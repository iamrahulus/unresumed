export default function SmokeTest() {
  return (
    <div style={{padding:24}}>
      <h1>✅ React mounted</h1>
      <p>If you can read this, mounting is fine. Any blank screen is from the app code/imports.</p>
    </div>
  );
}

